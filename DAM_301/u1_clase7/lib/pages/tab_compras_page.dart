import 'package:flutter/material.dart';

class TabComprasPage extends StatelessWidget {
  const TabComprasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Compras',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
