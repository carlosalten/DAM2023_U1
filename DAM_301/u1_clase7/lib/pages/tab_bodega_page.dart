import 'package:flutter/material.dart';

class TabBodegaPage extends StatelessWidget {
  const TabBodegaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Bodega',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
