import 'package:flutter/material.dart';

class TabVentasPage extends StatelessWidget {
  const TabVentasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Ventas',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
